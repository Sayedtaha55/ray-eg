import React from 'react';

type Props = {
  open: boolean;
  onClose: () => void;
  shopId: string;
  products: any[];
};

const ShopImageMapEditorModal: React.FC<Props> = () => {
  return null;
};

export default ShopImageMapEditorModal;

# ----------------------------------------------------------------
# EXAMPLE ENVIRONMENT VARIABLES
# Copy this file to .env.local and fill in your actual values.
# ----------------------------------------------------------------

# --- Application ---
NODE_ENV=development

MEDIA_STORAGE_MODE=local

PORT=4000
BACKEND_PORT=4000

# --- Database (for local development with Prisma) ---
# For PostgreSQL (recommended): DATABASE_URL="postgresql://ray_user:ray_password@localhost:5432/ray_marketplace?schema=public"
# For SQLite (legacy; requires backend/prisma/schema.prisma): DATABASE_URL="file:./dev.db"
DATABASE_URL="postgresql://ray_user:ray_password@localhost:5433/ray_marketplace?schema=public"

# --- JWT Configuration ---
# Generate a secret here: https://www.allkeysgenerator.com/Random/Security-Encryption-Key-Generator.aspx
JWT_SECRET=your-super-secure-jwt-secret-key-min-32-chars
JWT_EXPIRES_IN=7d


# --- URLs ---
# URL the backend uses to redirect back to the frontend
FRONTEND_APP_URL=http://localhost:5173
# URL the frontend runs on
FRONTEND_URL=http://localhost:5173
# URL the frontend (Vite) uses to talk to the backend
VITE_BACKEND_URL=http://127.0.0.1:4000
VITE_PORT=5173
# CORS Origin for backend to allow requests from frontend
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

AUTO_APPROVE_MERCHANTS_IN_DEV="true"
ALLOW_DEV_ADMIN_BOOTSTRAP="true"
ALLOW_DEV_MERCHANT_BOOTSTRAP="true"

# --- Redis (For Docker setup) ---
# Use REDIS_URL for Railway/upstash (overrides REDIS_HOST/PORT if set)
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379

# --- Admin Bootstrap (Production only) ---
# Secure token for initial admin creation via POST /api/v1/auth/bootstrap-admin
ADMIN_BOOTSTRAP_TOKEN=change-this-to-secure-random-string-in-production
# Set to true only if you need to re-run/reset admin bootstrap
ADMIN_BOOTSTRAP_ALLOW_RESET=false



